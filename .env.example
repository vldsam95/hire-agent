# Production server port
PORT=8080

# Real chat gateway. Never put this key in the browser or commit .env.
OPENAI_API_KEY=replace-with-your-server-side-key
# Optional. Defaults to gpt-5.
OPENAI_MODEL=gpt-5
